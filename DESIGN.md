# 디자인 — 무엇을 따랐고 무엇을 벗어났나

이 도구의 화면은 「디자인 지침 — THINK Lab 해석도구 공통」을 따른다.
토큰·컴포넌트·레이아웃은 지침 그대로이고, 아래 세 가지만 이 도구의 사정에 맞춰
바꿨다. 지침이 시킨 대로 **이유를 함께** 적어 둔다.

## 그대로 따른 것

| 규칙 | 이 도구에서 |
|---|---|
| 흰 바닥, 단일 라이트 테마 | 다크 테마와 테마 토글을 없앴다 |
| 깊이는 그림자만 | 패널·그룹·버튼의 테두리선을 전부 `--sh-1/2/3` 으로 바꿨다 |
| 강조색 하나 (navy `#0f4c81`) | 측정 벡터·슬라이더·주 버튼·활성 세그먼트가 모두 navy |
| 사이드바 352 px + 뷰포트 | 한 화면에 들어가고 스크롤은 사이드바에서만 |
| 접힌 그룹은 자기 상태를 말한다 | `유동`·`촬영 조건`·`계산 조건`·`보기`·`내 영상` 모두 `.gstate` 한 줄 요약 |
| 슬라이더 + 숫자칸 + 유도값 힌트 | 수치 파라미터 여섯 개 전부. 선택 목록에도 힌트를 붙였다 |
| 경고는 물리 기준으로, 값 옆에서 | 힌트가 즉시 색으로 경고하고, 오른쪽 아래에 **한 번에 하나만** 뜬다 |
| 산출물은 파일 하나, CDN 금지 | 웹폰트 링크를 없앴다. `dist/piv-simulator.html` 은 외부 요청이 0 이다 |
| 스칼라장은 viridis | 속도·오차·상관면 모두 viridis |

경고 사슬은 심각한 순서로 검사하고 가장 급한 하나만 띄운다 —
1/4 규칙 → 창당 입자 수 → 유효 벡터율 → 상관 신호 → 입자상 지름.
각 경고는 문제만 말하지 않고 **쓸 수 있는 값**을 준다
(예: "Δx 를 8.0 px 이하로 줄이거나 창을 64 px 로 키우세요").

## 벗어난 것 세 가지

**1. 헤더의 2분할을 「설정 / 결과」 대신 4단계로 뒀다.**
지침은 "지금 도구의 어느 쪽에 있는가"를 늘 보이게 하라고 한다. 이 도구는
설정과 결과가 갈리지 않는다 — 촬영 조건을 바꾸면 결과가 바로 다시 나온다.
대신 PIV 를 모르는 사람이 따라갈 순서(씨딩 → 이중 노출 → 상호상관 → 속도장)가
있어서, 같은 세그먼트 컴포넌트로 그 네 단계를 뒀다. 누르면 그 단계가 보이는
화면으로 바뀌고, 헤더 오른쪽 한 줄이 그 단계를 설명한다.

**2. 입자 영상을 반전해서 보여 준다.**
실제 PIV 원본은 검은 바탕에 밝은 입자다. 그대로 두면 흰 바닥 규칙과 부딪히고,
navy 화살표가 검은 바탕에서 읽히지 않는다. 논문에 실을 때도 검은 바탕은 잉크를
먹어서 반전해 싣는 것이 관행이라, 화면에서도 반전한다. 밝기 범위는 고정하지
않고 영상 히스토그램(바탕 20 백분위 ~ 입자 99.5 백분위)에서 잡는다 — 시트
두께나 씨딩을 바꿔도 바탕이 회색으로 뜨지 않는다.
**상관 계산은 언제나 반전 전의 원본 값으로 한다.** 평균을 뺀 상관은 부호를
뒤집어도 답이 같으므로(conj(−A)(−B) = conj(A)B) 표시 극성은 숫자에 영향이 없다.

**3. 부호가 있는 장에는 발산형 색을 쓴다.**
와도와 발산은 부호가 뜻을 갖는다. viridis 로 그리면 시계/반시계가 구분되지
않으므로, 차트 계열색의 두 끝(`--c1` navy ↔ `--c6` brick)에 중성 중간색을 둔
발산형을 쓴다. 무지개는 쓰지 않는다.
여기에 하나 더 — **범위에 하한을 뒀다.** 와도는 벡터장을 미분한 값이라 잡음이
증폭된다. 범위를 자동으로만 잡으면 참값이 0 인 퍼텐셜 유동이 요란한 그림이
된다. 측정 잡음이 채울 수 있는 만큼(√2·σ/간격의 여덟 배)을 하한으로 두고,
하한이 걸리면 범례에 그 사실을 적는다.

## 다음 도구에 옮길 때

`css/piv.css` 의 `:root` 토큰 블록과 컴포넌트 CSS(그룹·컨트롤·세그먼트·체크·
버튼·패널)는 이 도구에 묶인 것이 없다. 그대로 복사하면 된다.
